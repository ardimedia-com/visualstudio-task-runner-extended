<DataTemplate xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:vs="http://schemas.microsoft.com/visualstudio/extensibility/2022/xaml"
              xmlns:styles="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
              xmlns:colors="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0">
    <Grid>
        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource {x:Static styles:VsBrushes.WindowTextKey}}" />
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Task Runner Extended — Phase 1 Spike"
                   FontSize="13"
                   FontWeight="SemiBold"
                   Margin="8,8,8,4" />

        <!-- Spike 1: TreeView with HierarchicalDataTemplate -->
        <!-- Spike 2: ContextMenu on tree items -->
        <TreeView Grid.Row="1"
                  ItemsSource="{Binding SpikeTreeItems}"
                  Margin="4"
                  Background="{DynamicResource {x:Static styles:VsBrushes.ToolWindowBackgroundKey}}">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <TextBlock>
                        <TextBlock.ContextMenu>
                            <ContextMenu>
                                <MenuItem Header="Start"
                                          Command="{Binding DataContext.SpikeContextMenuCommand, RelativeSource={RelativeSource AncestorType=TreeView}}"
                                          CommandParameter="{Binding Name}" />
                                <MenuItem Header="Stop"
                                          Command="{Binding DataContext.SpikeContextMenuCommand, RelativeSource={RelativeSource AncestorType=TreeView}}"
                                          CommandParameter="{Binding Name}" />
                                <Separator />
                                <MenuItem Header="Add to Group..."
                                          Command="{Binding DataContext.SpikeContextMenuCommand, RelativeSource={RelativeSource AncestorType=TreeView}}"
                                          CommandParameter="{Binding Name}" />
                            </ContextMenu>
                        </TextBlock.ContextMenu>
                        <Run Text="{Binding Icon, Mode=OneWay}" FontWeight="Bold" />
                        <Run Text=" " />
                        <Run Text="{Binding Name, Mode=OneWay}" />
                    </TextBlock>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

        <!-- Status bar -->
        <TextBlock Grid.Row="2"
                   Text="{Binding StatusText}"
                   Margin="8,4,8,8"
                   Opacity="0.7" />
    </Grid>
</DataTemplate>
